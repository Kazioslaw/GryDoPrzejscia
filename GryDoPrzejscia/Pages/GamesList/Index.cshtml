@page
@model GryDoPrzejscia.Pages.GamesList.IndexModel
@{
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Lista gier do przejścia</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-page="create" class="btn btn-primary">
                <i class="bi bi-database-add"></i> &nbsp;Add new Game
            </a>
        </div>
    </div>
    <br /><br />
    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>
                    Game Name
                </th>
                <th>
                    Launcher Name
                </th>
                <th>
                    Gram
                </th>
                <th>
                    Skończyłem
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var obj in Model.GamesList)
            {
                <tr>
                    <td>
                        @obj.Name
                    </td>
                    <td>
                        @obj.Launcher
                    </td>
                    <td>
                        <input type="checkbox" @(obj.isPlayed ? true : false)>
                    </td>
                    <td>
                        <input type="checkbox" @(obj.IsFinished ? true : false)>
                    </td>
                </tr>
            }       
        </tbody>
    </table>
</div>
